import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "'NEXON Lv1 Gothic OTF', sans-serif",
  },
  listitem: {
    fontFamily: "'NEXON Lv1 Gothic OTF', sans-serif",
  },
});

export default theme;
